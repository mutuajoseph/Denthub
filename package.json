{
  "name": "dentisthub",
  "private": true,
  "version": "0.1.0",
  "description": "DentistHub — FastAPI + React/Vite monorepo",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"pnpm run dev:backend\" \"pnpm run dev:frontend\"",
    "dev:backend": "cd backend && uv run uvicorn app.main:app --reload --port ${BACKEND_PORT:-8000}",
    "dev:frontend": "pnpm --filter frontend dev",
    "setup": "uv sync --project backend && pnpm install",
    "build": "pnpm --filter frontend build",
    "lint": "pnpm --filter frontend lint && cd backend && uv run ruff check ."
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
